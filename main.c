#include <stdio.h>
#include <stdlib.h>
#include"myFunction.h"
#include"immobiliere.h"

int main()
{
     /* USER utilisateur;

     FILE* fichier = fopen(FICHIER_UTILISATEURS, "ab");
    if (fichier == NULL) {
        printf("Erreur lors de l'ouverture du fichier.\n");
        return;
    }
    strcpy(utilisateur.nom, "Coulibaly");
    strcpy(utilisateur.prenom, "Drissa");
    strcpy(utilisateur.login, "Nymosdt");
    strcpy(utilisateur.password, "1TontonC");
    chiffrementMotDePasse(utilisateur.password);
    strcpy(utilisateur.email, "dc377303");
    utilisateur.typeUser = 1;
    fwrite(&utilisateur, sizeof(USER), 1, fichier);

    fclose(fichier);

     FILE* fichierBai = fopen(FICHIER_BAILLEUR, "ab");
    if (fichier == NULL) {
        printf("Erreur lors de l'ouverture du fichier.\n");
        return;
    }
    fclose(fichierBai);
     FILE* fichierClie = fopen(FICHIER_CLIENT, "ab");
    if (fichier == NULL) {
        printf("Erreur lors de l'ouverture du fichier.\n");
        return;
    }
    fclose(fichierClie);
     FILE* fichierCon = fopen(FICHIER_CONTRAT, "ab");
    if (fichier == NULL) {
        printf("Erreur lors de l'ouverture du fichier.\n");
        return;
    }
    fclose(fichierCon);
    FILE* fichierLo = fopen(FICHIER_LOCATION, "ab");
    if (fichier == NULL) {
        printf("Erreur lors de l'ouverture du fichier.\n");
        return;
    }
    fclose(fichierLo);
    FILE* fichierPa = fopen(FICHIER_PAIEMENT, "ab");
    if (fichier == NULL) {
        printf("Erreur lors de l'ouverture du fichier.\n");
        return;
    }
    fclose(fichierPa);*/

    menu();

    return 0;
}
